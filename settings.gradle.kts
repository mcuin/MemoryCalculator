pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Memory_Calculator"
include(":androidMemoryCalculator")
include(":sharedMemoryCalculator")